apiVersion: v1
kind: Secret
metadata:
  name: funbot-proxies
  namespace: funbot
  labels:
    app: funbot
type: Opaque
stringData:
  # One proxy per line: socks5://host:port or socks5://user:pass@host:port
  proxies.txt: |
    # Add proxy entries here, one per line:
    # socks5://proxy1.example.com:1080
    # socks5://user:pass@proxy2.example.com:1080
